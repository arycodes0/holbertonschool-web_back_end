#!/usr/bin/env python3
"""This module contains a function that takes a float as arg
and returns the str representation of the float."""


def to_str(n: float) -> str:
    """Returns the string representation of the float n."""
    return str(n)
